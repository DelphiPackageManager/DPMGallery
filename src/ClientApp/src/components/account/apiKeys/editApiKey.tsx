import { ApiKey } from "../../../types";

export type EditApiKeyProps = {
  apiKey: ApiKey;
};

const EditApiKey = (props: EditApiKeyProps) => {
  return <h3>Edit</h3>;
};

export default EditApiKey;
